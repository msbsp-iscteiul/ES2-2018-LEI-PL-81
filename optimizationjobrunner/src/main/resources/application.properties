# Spring Datasource (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/Backend?useUnicode=true&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.jndi-name=java:comp/env/jdbc/iscte_es
# spring.datasource.jmx-enabled=true
# spring.datasource.tomcat.data-source=org.apache.tomcat.jdbc.pool.DataSourceFactory
# spring.datasource.tomcat.driver-class-name=com.mysql.jdbc.Driver
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=50
# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true

# Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

server.port = 8090

# Mail

spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=894d31b3ee68c2
spring.mail.password=23b8415d5dea92
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

mail.admin=admin@optimizations.com

# JMetal
jmetal.experiment_base_directory=experimentBaseDirectory
jmetal.reference_fronts=experimentBaseDirectory/referenceFronts
jmetal.experiment_name=Experiment

# Backend
backend.host=127.0.0.1
backend.port=8080
